# 旧插件迁移

## 提示词模板

1. 从旧插件导出提示词模板 JSON。
2. 打开新插件设置页。
3. 进入“提示词模板”。
4. 点击“导入”并选择 JSON 文件。

新插件兼容旧格式的 prompt 字段，并迁移到新的 content 字段。无效记录不会静默丢弃，界面会显示成功和失败数量。

## 模型配置

浏览器扩展之间无法跨 Extension ID 自动读取 chrome.storage。API 地址和模型可以通过团队配置迁移，API Key 必须重新输入。

## 历史

旧插件的一次性结果不会自动迁移。新插件只保存启用后的最近 50 条成功记录。
