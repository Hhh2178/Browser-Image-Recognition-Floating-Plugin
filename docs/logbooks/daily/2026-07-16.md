# 2026-07-16

## Intent

Continue the browser image analysis extension iteration and establish a durable project harness for future synchronized GitHub releases.

## Changed

| Area | Files | Purpose |
| --- | --- | --- |
| Image analysis | `src/features/media/`, `entrypoints/content/index.tsx` | Prepare remote and thumbnail images as model-friendly data URLs |
| Provider diagnostics | `src/features/analysis/` | Parse more response shapes and expose better API error details |
| Prompt/settings | `src/features/prompts/`, `src/features/workbench/` | Preserve and migrate prompt presets and model/provider settings |
| History export | `src/features/history/`, `entrypoints/background.ts` | Export only unexported successful prompt results to TXT |
| Governance | `AGENTS.md`, `docs/INDEX.md`, `docs/governance/`, `docs/systems/`, `scripts/verify-harness-contract.mjs` | Establish harness and future release registration |

## Verification

| Check | Result | Notes |
| --- | --- | --- |
| `npm run harness:verify:project` | Passed | Harness project contract is present |
| `npm run harness:verify:release` | Passed | Release log exists for `v0.2.0` |
| `npm run check` | Passed | ESLint, TypeScript, 51 Vitest tests, production build, manifest assertion |
| `npm run test:e2e` | Passed | 7 Playwright extension flows |

## Decisions

- Use lightweight doc-log harness mode.
- Use `0.2.0` for this release because the current changes include multiple user-visible features.
- Keep full automatic extension overwrite updates out of scope until separately approved.

## Risks

- GitHub Release page may still need to be created after pushing tag if downloadable assets should be attached.
- Chrome unpacked extension updates still require rebuild and reload unless a packaged distribution/update channel is added.

## Next

Commit, tag, and push `v0.2.0` to GitHub.
