# Release v0.2.0 - GitHub

## Scope

- Multi-provider and multi-model configuration improvements.
- Daily usage cap based model fallback behavior.
- Floating workbench UI refinements with inline settings.
- Multi-task analysis queue.
- Remote/thumbnail image preparation to JPEG Data URLs.
- Legacy prompt preset migration.
- Incremental TXT export for successful unexported prompt results.
- Harness documentation and verification scripts.

## Commits

Release commit is tagged by `v0.2.0`.

## Migration

User settings, API keys, prompt presets, history, and export markers remain in local Chrome extension storage/IndexedDB. The release must not overwrite user-created presets or provider settings.

## Verification

- `npm run harness:verify:project` passed.
- `npm run harness:verify:release` passed.
- `npm run check` passed: ESLint, TypeScript, 51 Vitest tests, production build, and manifest permission assertion.
- `npm run test:e2e` passed: 7 Playwright extension flows.

## Rollback

Reload the previous unpacked `.output/chrome-mv3` build or check out the prior Git tag/commit and rebuild.

## Incidents

None recorded.

## Final State

Ready for GitHub branch push and tag push.
